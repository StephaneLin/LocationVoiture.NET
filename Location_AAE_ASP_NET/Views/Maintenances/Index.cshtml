@model IEnumerable<Location_AAE_ASP_NET.Models.Maintenance>

@{
    ViewBag.Title = "Maintenances";
}

<h2>Maintenances</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tire)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.framework)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.electronics)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tire)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.framework)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.electronics)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PK_maintenanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PK_maintenanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PK_maintenanceId })
        </td>
    </tr>
}

</table>
